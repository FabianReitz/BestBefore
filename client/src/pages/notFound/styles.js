import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    root: {
        flexGrow: 1,
    },

    menuButton: {
        marginRight: '20px',
    },

    title: {
        flexGrow: 1,
    },
    image: {
        maxHeight: '218px',
        maxWidth: '500px',
        textAlign: 'center',
    },
}));
