import React from 'react';

import useStyles from './styles';

const Item = () => {

    // eslint-disable-next-line
    const classes = useStyles();
    
    return(
        <h1>ITEM</h1>
    );
};

export default Item;